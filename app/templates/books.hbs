{{#if showAll}}
  <button {{action 'showAll'}} class='button'>Show All</button>
{{/if}}

<ul>
  {{#each model as |book|}}
    {{book-cover book=book blurBackground=(route-action 'blurBackground')}}
  {{/each}}
</ul>
